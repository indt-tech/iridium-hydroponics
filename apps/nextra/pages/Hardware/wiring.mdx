
# Electrical Wiring

You can follow this scheme for the electrical wiring:

import React, {useState} from 'react';

export function Dialog(props) {
  const [isDialogOpen, setDialogOpen] = useState(false)

  const toggleDialog = () => {
    setDialogOpen(!isDialogOpen)
  }
  if (!isDialogOpen) return (<div style={{cursor:"zoom-in"}} onClick={toggleDialog}>{props.children}</div>)
  return (
    <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.8)', display:'flex', alignItems:'center', justifyContent:'center' }} onClick={toggleDialog}>
      <div style={{ backgroundColor: 'white', marginTop: '4%', padding: '20px', height:'90vh', width:'90vw', maxWidth: '1100px', display:'flex', alignSelf:'center'}} onClick={e => e.stopPropagation()}>
        {props.children}
      </div>
    </div>
  )
}

<Dialog>
![Protofy](http://localhost:8080/media/wiring.png?raw=true)
</Dialog>

![Protofy](http://localhost:8080/media/iridium/iridium_electronicbox2.jpg?raw=true)